# Prize icons (optional)

`data/boxes.json` uses short **emoji** icons by default (no broken images).

To use image icons later, place PNGs here and set item `icon` to a relative path such as `icons/mount.png`. The overlay loads paths / URLs as `<img>`; emoji strings render as large text; missing icons fall back to a rarity gem.
